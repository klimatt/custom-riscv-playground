fn main() {
    /*let map_folder = "map";
    let bin_folder = "bin";
    let map_file = "output.map";
    let bin_name = "output.bin";

    let (linker_arg, gen_bin_arg) = (
        format!("-Map={}/{}", map_folder, map_file),
        format!("-o{}/{}", bin_folder, bin_name),
    );

    println!("cargo:rustc-link-arg={}", linker_arg);
    println!("cargo:rustc-link-arg={}", gen_bin_arg);
    println!("cargo:rustc-link-arg=-Tlink.x");
    println!("cargo:warning=Arguments: {}, {}", linker_arg, gen_bin_arg);
    */
}
